// dsh-turnbar client half 占位：D3/D4 在此实现全景进度条 + 悬停预览卡
// （经官方 conversation.composer.dock 插槽注入，React 由 client runtime 提供）。
export default {
  name: 'dsh-turnbar-client',
  inject: [] as string[],
  apply(_ctx: unknown): () => void {
    return () => {}
  },
}
